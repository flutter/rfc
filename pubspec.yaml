name: rfc_tools
description: Tooling, validation, and linting for Flutter RFC repository.
version: 0.1.0
publish_to: 'none'

environment:
  sdk: '>=3.12.0 <4.0.0'

dependencies:
  args: ^2.5.0
  clock: ^1.1.2
  file: ^7.0.0
  path: ^1.9.0
  yaml: ^3.1.2

dev_dependencies:
  lints: ^6.1.0
  test: ^1.25.0
